<view class='public'>
    <view class='public_list' wx:for="{{publicData}}" data-type='{{item.type}}' bindtap='goTypeUrl' wx:key="{{index}}">
      <image src='{{item.imgUrl}}' ></image>
      <text>{{item.title}}</text>
    </view>
</view>